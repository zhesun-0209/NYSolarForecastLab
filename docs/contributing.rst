Contributing
============

Before submitting changes, verify the command-line interface and tests:

.. code-block:: bash

   python run.py --help
   python run.py config
   python -m pytest -q

Changes to data processing, feature definitions, metrics, or model interfaces
should include focused tests and documentation updates.

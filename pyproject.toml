[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-ra"
filterwarnings = [
    "ignore::UserWarning",
    "ignore::FutureWarning",
]

[tool.black]
line-length = 100
target-version = ["py310"]
